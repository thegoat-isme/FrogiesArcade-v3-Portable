services:
  - type: web
    name: my-type-is-the-bag
    runtime: node # or your specific runtime
    buildCommand: npm install && npm run build
    startCommand: npm start
    # No 'repo' field needed if it's in the same repository

